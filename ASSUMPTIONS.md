# ASSUMPTIONS — 문서에 없어서 임의 결정한 사항

> 형식: `[Phase] 결정 내용 — 근거` (룰 관련 결정은 엔진 테스트를 함께 추가한다)

- [Phase 0] 저장소 루트를 모노레포 루트로 사용 — PLAN.md §1.1의 `cheongiwa/` 디렉터리가 곧 이 리포지토리 루트.
- [Phase 0] 브랜치 운영: 호스팅 환경 지침에 따라 지정 브랜치(`claude/cheongiwa-riichi-mahjong-6xko42`) 하나에서 개발 — §10.3의 "Phase별 브랜치 후 머지" 대신 conventional commits 단위만 유지.
- [Phase 0] 폰트(Noto Serif KR·Pretendard) 로컬 번들은 Phase 4에서 도입 — 그때까지 시스템 폰트 폴백 사용, 외부 CDN 미사용 원칙(§4.2)은 준수.
- [Phase 0] 포트: 서버 8787(`PORT` env로 변경), 클라 개발 서버 5173 — vite 프록시로 same-origin 통신하여 CORS 설정 불필요.
- [Phase 0] Node 22 LTS를 CI·Docker 기준으로 사용 — 요구 최저선은 문서대로 Node 20 (`engines.node >= 20`).
- [Phase 0] Tailwind CSS v4(CSS-first) 채택 — §4.1 토큰은 `:root` CSS 변수(문서 표기 그대로 `--ink` 등)로 정의하고 `@theme inline`으로 유틸리티에 매핑.
- [Phase 0] 서버 프로덕션 빌드는 tsup CJS 단일 번들(워크스페이스 패키지 포함 전부 번들) — 런타임 node_modules 불필요. Phase 2에서 better-sqlite3(네이티브 모듈) 도입 시 external 전환 예정.
- [Phase 0] 패 표기는 mpsz 표기법(`1m~9m / 1p~9p / 1s~9s / 1z~7z`, 적5는 `0m·0p·0s`)을 로그·테스트 픽스처 표준으로 사용.
- [Phase 0] 프로토콜 버전 상수 `PROTOCOL_VERSION = 1`에서 시작 — 이벤트·스냅샷 스키마의 파괴적 변경마다 +1.
- [Phase 0] 패키지 스코프는 `@cheongiwa/*` (engine · protocol · server · client).
- [방향 개정 2026-07-24] 사용자 결정으로 캐릭터를 순수 비주얼 요소(과금·가챠·판정 영향 없음)로 도입 — 상세는 `ART-DIRECTION.md`. PLAN.md §0의 "캐릭터 없음"은 이 범위에서 개정됨.
- [Phase 1] 패산 물리 배치(주사위·패산 가르기)는 생략하고 논리 순서로 구현 — 셔플된 136 순열에서 배패 52 / 라이브 70 / 왕패 14(영상 4 + 도라표시 5 + 우라 5). 깡마다 라이브 잔여 −1로 해저 당김을 표현 (동등 모델, 시드 재현성 동일).
- [Phase 1] 유국 텐파이 판정: 형식 텐파이 인정하되, 대기 전부를 자기 손에 4장 들고 있는 순수 카라텐은 노텐 처리 (표준 관례). 리치 선언 가능 여부는 형식 텐파이(샹텐 0) 기준.
- [Phase 1] 리치 후 안깡 허용 기준: 쯔모패와 같은 종류 + 깡 전후 대기 목록 불변 (오쿠리깡 방지의 표준 구현).
- [Phase 1] 쿠이카에(울고 즉시 되버림) 금지: 울어간 패와 같은 종류 + 양끝 치의 스지 반대끝을 그 타패에 한해 금지 (표준 관례, PLAN 미기재 → 채택). 테스트 포함.
- [Phase 1] 찬타·준찬타는 슌쯔 1개 이상을 요구 (커쯔만으로 구성되면 혼로두·청노두 계열로만 취급) — 표준 관례.
- [Phase 1] 대명깡·가깡 신도라의 '타패 시 공개'는 그 타패 직후(론 판정 이전) 공개로 구현 — 그 타패에 론이 나면 새 도라가 반영됨 (통용 구현).
- [Phase 1] 나가시만관은 만관 쯔모 상당 지급으로 처리, 성립 시 노텐벌부 없음, 연친 여부는 통상 유국과 동일하게 친 텐파이 기준. 복수 성립 시 전원 지급.
- [Phase 1] 파오 지불: 쯔모 시 파오 대상이 역만 전액+본장 부담, 론 시 방총자와 절반씩(본장은 방총자) — 해당 역만 외 복합분은 통상 지불. 더블론 시 본장·공탁은 첫(가까운) 화료자만.
- [Phase 1] 국사무쌍 13면·스안커 단기·순정구련·대사희 외의 복합(예: 대사희+스안커단기)은 각 배수 합산 (multipleYakuman ON 기준). OFF면 최고 배수 1개만.
- [Phase 1] 깡은 대국 전체 4회 상한 (1인 4깡 포함 — 영상패가 4장이므로). 라이브 패산 0장이면 깡·치·퐁 불가 (하저 타패는 론만).
- [Phase 1] 사가리치·사깡산료·사풍연타 판정 시점: 해당 타패가 론 없이 통과된 직후. 우선순위는 사가리치 > 사깡산료 > 사풍연타.
- [Phase 1] 아가리야메는 도중유국에는 적용하지 않음 (화료·텐파이 유국만).
- [Phase 1] 서든데스(서입) 구간에서도 아가리야메 조건이 성립하면 종료 선택 가능. 서4 종료 시에는 30,000 미달이어도 무조건 종료.
- [Phase 1] 봇 v1 정책: 화료·리치 항상, 구종구패 항상 선언, 역패 퐁(리치자 없을 때), 이미 오픈이면 샹텐 주는 치, 대기 불변 안깡·가깡, 리치자 있고 2샹텐 이상이면 현물 수비, 동률 타패는 요구패→낮은 종류 순 (결정론).
- [Phase 1] 울기 후 쿠이카에 금지로 버릴 수 있는 패가 하나도 없어지는 울기(예: 3456m에서 3m을 45m로 치)는 선택지에서 제외 — 표준 관례 (버릴 패가 없는 울기는 불가). 10,000반장 시뮬레이션이 발견한 교착 사례로 회귀 테스트 포함.
- [Phase 2] §3.3 표 외 보조 이벤트 추가: `lobby.practice`(연습 시작) · `lobby.fillOffer/fillAccept`(봇 충원 문답) · `room.start` · `settings.auto`(§3.4 자동 편의) · `server.hello/auth.welcome/room.state/lobby.queue/server.error` — 표의 흐름을 구현하는 데 필요한 최소 확장.
- [Phase 2] 국 결과 후 서버가 자동으로 다음 국 진행 (기본 8초, `RESULT_DELAY_MS`) — "계속" 버튼은 Phase 3 클라 연출(스킵)로 처리. 아가리야메 조건 성립 시 서버가 자동으로 종료 선택 (유저 선택 UI는 Phase 3 검토).
- [Phase 2] 좌석 배정은 대국 시작 시 crypto 무작위, 방은 시작과 함께 해산 (한 번 더는 새 방).
- [Phase 2] 황패유국 시 텐파이자의 손패를 공개한다 (표준 관례) — `game.roundResult`에 포함.
- [Phase 2] 이탈 대체는 계획(§3.4)대로 안전봇(쯔모 화료 + 쯔모기리 + 전부 패스), 방·매칭의 빈자리 충원은 봇 v1. 재접속 창(5분) 초과 시 대국 종료까지 안전봇이 두고 전적에 탈주(completed=0) 기록.
- [Phase 2] 매칭 봇 충원 제안: 수락자끼리 묶어 봇을 채워 시작하고, 거절·무응답(10초)은 대기 유지(대기 시간 리셋).
- [Phase 2] 게스트 토큰은 SHA-256 해시로만 저장. 리치 중 선택지가 쯔모기리뿐이면 서버가 자동 진행 (통용 편의).
- [Phase 2] 시드 검증: 공개 시드 → 엔진 PRNG로 매 국 패산이 유도되므로 시드+공개 이벤트만으로 대국 전체를 재검증할 수 있다 (검증 뷰어는 Phase 5 리플레이에서).
- [Phase 2 버그수정] `botDelayMs>0`일 때 봇 액션은 setTimeout으로 예약되는데, 클라가 game.start 직후 보내는 `settings.auto`가 `step()`을 호출해 같은 봇 액션이 이중 예약되어 봇이 두 번 착수 → 서버 크래시하던 버그를 발견·수정 (예약 가드 + 봇 액션 try/catch 폴백). 회귀 테스트 추가. Phase 2 통합 테스트가 botDelayMs=0(동기 경로)만 써서 놓쳤던 케이스.
- [Phase 3] 클라 게임 상태는 서버 증분 이벤트를 로컬 뷰로 리듀스한다. 타인 손패는 항상 `13 − 3×부로` 장의 뒷면으로 파생 렌더(증분 추적 없이 항상 정확), 자기 손패만 game.deal/game.draw + 자기 타패·울기 이벤트로 갱신. 재접속은 sync.snapshot으로 전체 교체.
- [Phase 3] 마작패는 전통 공용 도안을 SVG로 렌더: 통·삭은 벡터 도형(원·대나무), 만·풍패·삼원패는 CJK 문자(一~九·萬·東南西北·發·中)를 serif로 렌더(전통 각인 도안, 유니코드 마작 문자🀇와 무관), 백은 빈 테두리. 이모지·유니코드 마작 문자는 전 화면 미사용.
- [Phase 3] 대국 화면 좌석 배치는 내 시점 상대 위치(self 하단·하가 우·대면 상·상가 좌)로 렌더, 버림패는 위치별 회전. 폰트 로컬 번들·처마/단청/도장 정밀 연출은 Phase 4 (기능 우선).
- [Phase 3] 국 결과 "계속"은 화면 클릭으로 오버레이만 닫고, 다음 국 진행은 서버 자동(RESULT_DELAY). 아가리야메 유저 선택 UI는 미구현(서버 자동 종료). 예상 점수 미리보기는 클라에서 evaluateWin으로 계산(우라 제외, 론 기준).
- [Phase 3] 최종 소프트 점수를 관례대로 `(점수−25,000)/1000 + 우마`(오카 없음, 전체 합 0)로 변경 — Phase 1의 `점수+우마×1000` 표기를 정정. FinalStanding.finalPoints 의미 변경, 엔진 테스트 갱신.
- [Phase 4] 폰트는 서브셋으로 로컬 번들 (§4.2, 총 782KB): Noto Serif KR은 소스에 실제 등장하는 글리프만(418자), Pretendard는 라틴+상용 한글. 생성 스크립트 `apps/client/scripts/build-fonts.mjs`를 리포에 두어 재현 가능하게 함. 서브셋에 없는 글자는 시스템 폰트로 폴백(닉네임 등 희귀 음절).
- [Phase 4] 효과음은 외부 샘플 없이 Web Audio 합성 (§4.6의 "적합한 국악 샘플이 없으면 타악·신스" 조항 적용). 라이선스 문제 0, 용량 0, 음색은 북·대금·가야금·태평소를 신스로 근사. BGM은 계획대로 MVP 범위 외.
- [Phase 4] 로비/대기방 배경은 원화 도착 전까지 벡터(밤하늘·달·능선·처마·꽃잎)로 구성. 원화가 들어오면 `NightSky`의 배경 레이어만 교체하면 되도록 분리 (ART-DIRECTION.md 핸드오프).
- [Phase 4] 캐릭터 슬롯은 아직 배치하지 않는다 — 벡터 실루엣으로 채우면 오히려 완성도가 떨어져, 원화 도착 시 함께 넣는다.
- [Phase 4] 성능: 결과 오버레이가 48fps로 떨어지던 원인 3가지를 수정 — 카운트업의 프레임당 React 리렌더(→ DOM 직접 갱신), 전체화면 backdrop-blur(→ 불투명 스크림), JS 구동 파티클(→ transform/opacity CSS 애니메이션). 전 구간 60fps 확인.
- [Phase 4 버그수정] 국 결과 표시 중 `step()`이 재진입하면 결과 타이머가 중복 예약되어 다음 국을 두 번 진행하려다 서버가 죽던 버그를 발견·수정(타이머 가드). 회귀 테스트 추가.
- [Phase 3] Playwright E2E(§8.2)는 로컬/수동 스크립트(`pnpm --filter @cheongiwa/client e2e`)로 두고 기본 CI에는 넣지 않는다 — GitHub Actions에 브라우저 설치가 필요해 파이프라인을 무겁게/불안정하게 만들기 때문. 단위·통합·빌드는 CI에서 계속 검증.
- [Phase 5] 엽전 보상 수치는 PLAN §6.2를 그대로 구현: 완주 반장 100 / 동풍 50, 순위 80·40·20·10, 오늘의 첫 대국 50, 역만 화료 기념 500(계정당 역별 1회), 빈곤 구제는 잔액 200냥 미만일 때 하루 1회 200냥까지 보충. 지급 조건은 "인간 2인 이상 · 연습 아님 · 본인 완주"이며, 멱등 키는 `(user_id, game_id)` — 재정산해도 이중 지급되지 않는다.
- [Phase 5] 엽전은 순위와 무관하게 전원이 얻기만 한다(4위도 +10). 잃는 경로는 상점 구매뿐 — 베팅·유저 간 이전은 스키마 수준에서 존재하지 않는다 (§6.1).
- [Phase 5] 등급은 유생(0)→진사(900)→급제(2100)→장원(3600) 누적 점수제. 순위별 증감은 등급이 오를수록 하위 순위 페널티가 커지고(유생 +60/+25/0/−15 … 장원 +50/+10/−30/−70), 점수 하한은 0이라 등급 강등은 없고 단계만 내려간다. 동풍전은 증감 절반. 반영 조건·멱등 키는 보상과 동일.
- [Phase 5] 상점 상품 14종은 전부 코스메틱(패 뒷면·마작상·화료 연출·이모티콘)이며 슬롯마다 무료 기본품이 하나씩 있어, 엽전이 0이어도 게임 경험이 제한되지 않는다. 가격은 §6.3 표 그대로.
- [Phase 5] 코스메틱 가시성 규칙: 패 뒷면과 화료 연출은 **소유자 기준**으로 모두에게 보이고(상대 손패 뒷면 = 그 사람이 산 것, 병풍 색 = 화료자가 산 것), 마작상은 **보는 사람 기준**(각자 자기 상 위에서 둔다). 이를 위해 `game.start`/`sync.snapshot`에 좌석별 공개 프로필(`tier`·`level`·`tileBack`·`winEffect`)을 실었다 — 은닉 정보는 아니며 판정에 쓰이지 않는다.
- [Phase 5] 역만 화료의 금박 병풍은 장착한 화료 연출보다 우선한다 — 역만이라는 사실이 코스메틱보다 먼저 읽혀야 하기 때문.
- [Phase 5] 보상 내역은 최종 결과 화면에서 보여주고, 거기서 확인한 경우 로비 플로팅은 띄우지 않는다. 로비 플로팅은 접속 시 지급되는 빈곤 구제처럼 결과 화면을 거치지 않는 지급에만 뜬다.
- [Phase 5 버그수정] 상대가 울어간 부로(면자)가 대국 화면에 전혀 렌더되지 않아 상대 손을 읽을 수 없던 문제를 발견·수정 — 좌석별 부로 앵커를 추가해 손패 바깥쪽에 표시한다 (안깡 뒷면은 그 좌석이 장착한 뒷면).
- [Phase 5] PLAN §7 Phase 5의 나머지 항목(리플레이 뷰어·관전·정식 계정·모바일 레이아웃·이모티콘 대화·삼마·대회 모드)은 이번 범위에서 제외 — 엽전·상점·등급까지가 "완성" 기준이었고, 나머지는 별도 페이즈로 다룬다.
- [Phase 4 후속] 원화를 OpenAI 이미지 API로 직접 생성한다. 모델은 투명 배경 지원 여부로 나눠 쓴다 — 배경은 `gpt-image-2`(구도·디테일이 가장 좋으나 `background=transparent`를 거부), 투명이 필요한 캐릭터·현판·아이콘은 `gpt-image-1.5`. 생성 API 최대 크기가 1536×1024라 배경은 그 해상도 그대로 두고 브라우저가 늘려 그리게 한다 (업스케일해도 디테일은 안 늘고 용량만 3배가 된다).
- [Phase 4 후속] 스타일 고정 문구를 장면용/오브젝트용으로 나눈다. 공용 문구에 "기와 지붕"을 넣었더니 인물 어깨 뒤와 아이콘 배경에까지 기와가 그려져 두 장을 버렸다. 오브젝트용에는 `plain empty background, no scenery, no buildings`를 명시한다.
- [Phase 4 후속] 생성물은 대상이 프레임 가운데 작게 놓이는 일이 많아, 파이프라인(`pnpm build-art`)에서 투명 여백을 잘라낸(trim) 뒤 WebP로 굽는다. 여백째 두면 CSS로 위치를 잡을 수 없다. 30MB → 1.9MB (95% 감소).
- [Phase 4 후속] 원화 생성은 결정론적이지 않으므로 `assets/handoff/` 의 원본을 리포에 커밋한다 (약 30MB). 스크립트만 있으면 다시 만들 수 있다는 말은 이 경우 성립하지 않는다 — 같은 프롬프트로도 다른 그림이 나온다.
- [Phase 4 후속] 대국 화면 배경은 별도 레이어(`-z-10`) 대신 화면 루트 요소의 `background-image`로 깐다. `position: relative`는 스태킹 컨텍스트를 만들지 않아 음수 z-index 자식이 부모 배경색 뒤로 가버린다.
- [Phase 4 후속] 이미지 API 조직 한도가 분당 5장이라 동시 4개로 돌리면 429가 쏟아진다. 재개할 때는 `--concurrency 1`로 낮춘다. 예산 초과를 막는 `--budget` 상한도 추가했고, 예산 때문에 건너뛴 항목은 조용히 빠지지 않고 목록으로 출력한다.
- [Phase 4 후속] 품질 등급을 항목별로 나눈다. 화면에서 가장 큰 캐릭터 전신만 high, 아이콘·프레임·버튼은 medium — 128~256px로 표시되므로 차이가 보이지 않는다. 전량 high는 medium의 4.3배 비용이다.
- [Phase 4 후속] 원화 UI 조각은 9-slice로 늘리지 않는다. 기와 지붕·구름 장식이 붙어 있어 늘리면 문양이 뭉개진다. 대신 비율을 고정하고 안쪽 영역(inset)에 콘텐츠를 얹는다 — `components/art.tsx` 의 공통 방식이다.
- [Phase 4 후속] 로비 캐릭터는 클릭으로 4인을 순환한다. 판정·매칭에 영향이 없는 순수 표시 요소이며 서버에 저장하지 않는다 (§6.1 "코스메틱은 승패에 영향 없음"과 같은 원칙).
- [Phase 4 후속] 로비 배경은 18~06시에 야경으로 자동 전환한다.
- [Phase 4 후속] 원화 UI 조각의 크기 비율을 CSS `aspect-ratio` 에 하드코딩했더니, 재생성으로 원화를 갈아끼우자 전부 깨졌다(제목 현판 2.74→2.54, 탭 2.07→1.50). 이미지를 일반 블록으로 두고 글자만 그 위에 절대 배치해 **비율은 이미지가 정하도록** 바꿨다. 컴포넌트가 들고 있는 값은 안쪽 여백(inset)뿐이다.
- [Phase 4 후속] 통일 재생성이 전부 개선인 것은 아니다. 아이콘·프레임·버튼·캐릭터는 광택이 빠지고 서로 붙어 개선됐지만, 현판 4종은 마구리·매듭·초롱·잉어 장식이 뭉개졌고 도라 슬롯판은 슬롯이 흰색이 되어 흰 마작패와 구분되지 않았으며 엽전 아이콘은 테두리가 은색이 되어 놋쇠 엽전으로 읽히지 않았다. 이 6장은 이전 원화로 되돌렸다. 재생성 전 원본을 백업해 두는 것이 전제다.
- [Phase 4 후속] 캐릭터는 품질을 high→medium 으로 낮추면서 넷이 서로 붙는 대신 개별 완성도(옷 문양·얼굴)를 잃었다. "따로 논다"가 문제였으므로 통일을 택했지만 맞바꾼 것이 있다.
- [Phase 5-이모티콘] 이모티콘은 낱개 판매로 정리했다. 원래 카탈로그는 "하회탈 8종·각시탈 8종" 팩 2개 + "없음"이었는데 실제 원화는 탈 6종 낱장이라 맞지 않았다. 탈 하나 = 상품 하나로 바꾸고, 하회 양반탈을 무료 기본으로 뒀다. "장착"은 팔레트 첫 자리에 올 탈을 고르는 뜻이고 **산 것은 전부 대국 중에 쓸 수 있다**.
- [Phase 5-이모티콘] 도배 방지는 서버가 좌석당 3초 쿨다운으로 막는다. 쿨다운에 걸린 전송은 오류가 아니라 **조용히 무시**한다(도배 방지이지 잘못된 요청이 아니므로). 클라도 같은 3초 동안 버튼을 잠가 눌러도 안 나가는 것을 눈으로 알려준다. 받는 쪽은 음소거로 아예 안 볼 수 있고 이 설정은 localStorage 에 남는다.
- [Phase 5-이모티콘] 보유 검증은 서버가 한다. 클라가 팔레트에 없는 id 를 보내도 거부된다.
- [Phase 5-모바일] 세로 화면 대응은 미디어 쿼리로 단계마다 값을 새로 박는 대신 **실제 크기를 재서 비례로 줄이는** 방식을 택했다(`hooks/useElementSize.ts`). 대국 화면은 손패(46px 고정 × 14장 = 662px)와 마작상 안쪽(자리 앵커는 %지만 패·정보판은 고정 px)이 모두 고정 px이라, 390px 화면에서 문서 폭이 690px로 밀려났었다. 손패는 가용 폭에서 한 장 폭을 역산하고, 마작상은 600px 논리 좌표계를 통째로 `scale()` 해 어떤 폭에서도 비율이 그대로 유지된다.
- [Phase 5-모바일] 마작상은 가로·세로 중 **작은 쪽**에 맞춘다. 폭만 보고 맞췄더니 폰을 눕혔을 때(844×390) 세로가 먼저 바닥나 판이 화면 밖으로 흘러넘쳤다. 같은 이유로 손패 한 장 폭 상한도 화면 높이에 비례시킨다 — 안 그러면 눕힌 화면에서 손패가 마작상보다 커 보인다.
- [Phase 5-모바일] `transform: scale()` 은 그리는 크기만 바꾸고 레이아웃 크기는 그대로다. 축소한 600px 판을 그냥 두면 `aspect-square` 바깥 상자를 600px로 밀어내 화면 한가운데가 텅 비었다. 안쪽 판을 `absolute` 로 띄워 흐름에서 빼야 바깥 상자가 제 크기를 지킨다.
- [Phase 5-모바일] flex 항목은 기본값(`min-width: auto`)에서 내용 폭 아래로 줄지 않는다. 폭을 재는 요소가 `width: 100%` 라도 조상 flex 항목에 `min-w-0` 이 없으면 잰 값이 화면 폭이 아니라 내용 폭이 되어 축소가 아예 걸리지 않는다.
- [Phase 5-모바일] 사이드 패널(설정·역 일람·기록)은 `hidden lg:block` 이라 폰에서 **닿을 방법이 아예 없었다**. 상단 바에 서랍 손잡이를 두고 우측 서랍으로 연다. 배경이 비치면 글씨가 안 읽혀 서랍 바닥은 불투명하게 깐다.
- [Phase 5-모바일] 세로 화면에서 마작상은 폭에 묶인 정사각이라 세로가 남는다. 남는 자리를 마작상 위아래로 나눠 가운데 앉히고, 손패는 늘 화면 아래(엄지 닿는 자리)에 고정한다. 콜 도장 버튼은 좁을 때 손패 오른쪽 대신 **위**로 올라간다.
- [Phase 5-모바일] 로비 보상 플로팅은 세로 화면에서 띄울 빈자리가 없다(위는 제목·잔액바, 아래는 현판 4장). 좁은 화면에서만 흐름 안에 끼워 넣어 현판을 가리지 않게 하고, md 이상에서만 좌상단에 띄운다.
- [Phase 5-모바일] E2E 에 세로 화면 회귀 테스트를 추가했다(`e2e/mobile.spec.ts`) — 로비·저잣거리·대국·눕힌 화면·320px 에서 가로 넘침이 없을 것, 서랍으로 사이드 패널에 닿을 것. 대국 화면 선택자는 `.aspect-square` 대신 `[data-board]` 로 바꿨다(레이아웃 클래스는 언제든 바뀐다).
