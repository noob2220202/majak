# 개발 계획서 — 「청기와」 (가칭): 한국 전통 컨셉 온라인 리치마작 웹게임
> 이 문서는 클로드 코드에게 전달하는 개발 지시서입니다. 이 문서 하나로 전체 프로젝트의 방향, 규칙, 디자인, 개발 순서를 판단할 수 있도록 작성되었습니다. 문서 전체를 읽은 뒤 §10의 진행 방식에 따라 Phase 0부터 시작하세요.
---
## 0. 프로젝트 개요
**만들 것**: 4인 리치마작(일본식 마작)을 온라인 멀티플레이로 즐길 수 있는 웹게임. 게임명 가칭 「청기와」.
**목표 품질**: 기존 상용 마작 게임들과 견줄 수 있는 수준의 시각 완성도와 게임필. 캐릭터·가챠·과금 요소 없이, 대국 경험 자체의 완성도(부드러운 애니메이션, 세련된 연출, 정확한 룰 구현, 쾌적한 멀티플레이)로 승부한다.
**디자인 방향**: 한국 전통 미학. 기와지붕의 곡선, 한복과 오방색의 색감, 단청 문양, 한지 질감, 전각 도장을 모티프로 한 오리지널 디자인. 기존 마작 게임(작혼 등)의 에셋·일러스트·사운드·고유 명칭을 복제하는 것은 절대 금지. 참고하는 것은 "화면에 어떤 정보가 배치되는가"라는 기능적 구조까지만 허용된다.
**핵심 결정 사항**:
| 항목 | 결정 |
|---|---|
| 플레이 형태 | 처음부터 온라인 멀티플레이 (서버 권위 구조) |
| 룰 | 4인 반장전 풀룰 (동풍전은 방 옵션) |
| 기술 스택 | React + TypeScript (클라), Node.js + Socket.IO (서버) |
| 캐릭터/과금 | 실제 과금 없음. 무료 재화 「엽전」 + 코스메틱 상점은 Phase 5에 구현 (§6에 설계 확정) |
| AI 봇 | 필요 (빈자리 채우기, 탈주자 대체, 연습 모드) |
| 대상 플랫폼 | 데스크톱 브라우저 우선 (1280px+), 모바일은 Phase 5 |
| 언어 | 한국어 우선, i18n 구조만 잡아둠 |
---
## 1. 기술 스택과 아키텍처
### 1.1 저장소 구조 (pnpm 모노레포)
```
cheongiwa/
├─ packages/
│  ├─ engine/        # 순수 TS 마작 엔진. 의존성 0, 결정론적, 서버·클라 공용
│  └─ protocol/      # 공유 타입: 이벤트, 상태 스냅샷, 룰 설정 스키마 (zod)
├─ apps/
│  ├─ server/        # Node 20+, Fastify(정적 서빙·헬스체크) + Socket.IO
│  └─ client/        # React 18 + TS + Vite
├─ ASSUMPTIONS.md    # 문서에 없어서 임의 결정한 사항 기록
├─ ATTRIBUTIONS.md   # 외부 에셋 출처·라이선스 기록
└─ RULES.md          # 구현된 룰 최종 요약 (유저용)
```
### 1.2 아키텍처 원칙
1. **엔진 분리**: 마작 규칙 전체(패산 생성, 진행, 판정, 점수계산)는 `packages/engine`의 순수 함수로 구현한다. React도 Socket.IO도 모른다. 입력(현재 상태 + 액션) → 출력(다음 상태 + 발생 이벤트)의 결정론적 구조로, 시드만 같으면 같은 대국이 재현된다. 이것이 테스트·리플레이·서버 권위의 기반이다.
2. **서버 권위 (server-authoritative)**: 패산 셔플, 배패, 모든 판정은 서버에서만 일어난다. 클라이언트는 "내가 볼 수 있는 정보"만 받고, "지금 가능한 행동 목록"도 서버가 계산해서 내려준다. 클라이언트는 렌더링과 입력 전달만 담당한다. 이 구조가 치트를 원천 차단한다.
3. **RNG**: 서버에서 `crypto` 기반 Fisher-Yates 셔플. 대국 시작 시 시드의 해시를 전원에게 공개하고, 대국 종료 후 시드 원본을 공개해 셔플이 조작되지 않았음을 누구나 검증할 수 있게 한다(공정성 증명 — 상용 게임에 없는 차별화 기능).
4. **렌더링**: DOM + CSS transform + SVG로 구현한다 (Canvas/WebGL 불필요). 패는 SVG, 애니메이션은 framer-motion. 60fps 목표.
5. **상태 관리**: 클라이언트는 zustand. 서버가 보내는 스냅샷·이벤트를 반영하는 단방향 구조.
6. **스타일**: Tailwind CSS + 디자인 토큰(CSS 변수). §5의 디자인 시스템 값을 토큰으로 정의하고 모든 색·간격·타이포는 토큰만 사용한다.
7. **배포**: Dockerfile 하나로 클라이언트 정적 파일 + WebSocket 서버를 단일 프로세스 서빙. fly.io/VPS 어디든 올릴 수 있게.
---
## 2. 마작 룰 사양 (4인 반장전 표준)
아래 사양이 기본값이다. "토글"로 표시된 항목은 친선방 룰 설정에서 변경 가능하게 구현하되, 기본값부터 완성한다. 여기 없는 세부 규칙을 임의로 결정했다면 반드시 `ASSUMPTIONS.md`에 기록할 것.
### 2.1 패 구성과 배패
- 총 136장: 만수·통수·삭수 각 1~9 × 4장 (108장) + 자패 7종(동남서북·백발중) × 4장 (28장).
- **적도라**: 만·통·삭 각각의 5 한 장을 적5로 교체 (총 3장). 적5는 도라 1로 계산. — 토글 (기본 ON)
- 배패: 각자 13장, 친(dealer)은 첫 쯔모로 14장째 시작. 왕패 14장 상시 유지 (영상패 4장 + 도라표시패 영역).
- 도라표시패 1장 공개로 시작. 깡마다 신도라 추가 공개 (최대 5장).
- 도라는 표시패의 "다음 패": 9 다음은 1, 자패는 동→남→서→북→동, 백→발→중→백 순환.
- **우라도라**: 리치 화료자만 확인. 깡우라 포함. — 토글 (기본 ON)
### 2.2 진행
- 동1국~남4국 반장전 (동풍전 옵션: 동1~동4).
- 시작 25,000점 / 순위 우마 +15 / +5 / −5 / −15 (오카 없음). — 우마 값 토글
- 턴: 쯔모 → (화료/깡/리치 선택) → 타패. 타패에 대해 타가가 론/퐁/깡/치 선언 가능.
- 우선순위: **론 > 퐁·깡 > 치**. 복수 론은 더블론 인정, **트리플론은 유국**. — 토글
- 더블론 시 공탁 리치봉·본장은 방총자 기준 반시계 방향으로 가까운 화료자가 가져간다.
### 2.3 울기 (치·퐁·깡)
- 치: 상가(왼쪽)의 타패만. 퐁·깡: 누구의 타패든.
- 깡 3종: 안깡(손패 4장), 대명깡(타패 + 손패 3장), 가깡(퐁한 면자에 4장째 추가).
- 깡 후: 영상패 쯔모 → 타패. 왕패 보충으로 라이브 패산 마지막 1장이 왕패로 이동 (해저 위치가 당겨짐).
- **신도라 공개 타이밍**: 안깡은 즉시, 대명깡·가깡은 그 후 타패 시 공개. — 토글 (즉시 공개 옵션)
- **가깡에 대한 창깡(론) 가능**. 안깡에는 국사무쌍만 창깡 가능. — 토글
- 사깡산료: 2인 이상이 합계 4번째 깡을 만들면 그 타패 통과 후 유국 (1인이 4깡이면 스깡즈 진행 가능하므로 유국 아님).
- 쿠이탕(울고 탕야오) 허용. — 토글
### 2.4 리치
- 조건: 멘젠 텐파이 + 점수 1,000점 이상 + 패산 잔여 4장 이상 (자신의 쯔모가 최소 1회 남음).
- 선언 시 1,000점 공탁. 리치봉은 화료자가 회수, 유국 시 다음 국으로 이월, 최종국 종료 시 남은 공탁은 1위가 가져간다.
- 리치 후: 타패 선택 불가 (쯔모기리 강제). 안깡은 "대기와 손패 구성이 변하지 않는 경우"에만 허용.
- **일발**: 리치 선언 후 자신의 다음 쯔모까지, 사이에 울기가 없으면 +1판.
- **더블리치**: 첫 순(울기 개입 없음) 리치 시 2판.
- 사가리치: 4인 전원 리치 선언 → 4번째 선언 타패가 론 없이 통과되면 유국.
### 2.5 후리텐
- 자신의 대기패 중 **하나라도** 자기 버림패에 있으면 모든 대기에 대해 론 불가 (쯔모 화료는 가능).
- 동순 후리텐: 자기 대기패를 다른 사람이 버렸는데 론하지 않으면, 자신의 다음 타패까지 론 불가.
- 리치 후 론을 놓치면(패스하면) 그 리치는 **영구 후리텐** (쯔모만 가능).
### 2.6 화료와 판정
- 화료 조건: 4면자 + 1머리 (또는 치토이츠 7쌍, 국사무쌍 형태) + **역 1개 이상**. 도라·적도라·우라도라는 역이 아니므로 도라만으로는 화료 불가.
- 머리 우선/고점법: 복수 해석이 가능한 손은 항상 **최고 점수가 되는 해석**을 채택한다 (판수 → 부수 순 비교).
- 파오(책임지불): 대삼원의 3번째 삼원패, 대사희의 4번째 풍패를 울려준 자가 해당 역만 점수를 책임진다 (쯔모 시 전액, 론 시 방총자와 절반). — 토글 (기본 ON)
### 2.7 야쿠 일람 (전체 구현 대상)
| 판수 | 역 | 멘젠 한정 | 울면 |
|---|---|---|---|
| 1판 | 리치 | ○ | — |
| 1판 | 일발 | ○ | — |
| 1판 | 멘젠쯔모 | ○ | — |
| 1판 | 핑후 | ○ | — |
| 1판 | 탕야오 | × | 1판 (쿠이탕) |
| 1판 | 이페코 | ○ | — |
| 1판 | 역패 (장풍·자풍·백·발·중, 중복 가산) | × | 1판 |
| 1판 | 창깡 / 영상개화 / 해저모월 / 하저로어 | × | 1판 |
| 2판 | 더블리치 | ○ | — |
| 2판 | 치토이츠 (25부 고정) | ○ | — |
| 2판 | 혼로두 / 토이토이 / 산안커 / 산깡즈 / 산색동각 / 쇼산겐 | × | 2판 |
| 2판 | 산색동순 / 일기통관 / 찬타 | × | 울면 1판 |
| 3판 | 혼일색 | × | 울면 2판 |
| 3판 | 준찬타 | × | 울면 2판 |
| 3판 | 량페코 | ○ | — |
| 6판 | 청일색 | × | 울면 5판 |
| 역만 | 국사무쌍 / 스안커 / 대삼원 / 소사희 / 자일색 / 청노두 / 녹일색 / 스깡즈 / 구련보등 / 천화(친) / 지화(자) | 형태별 | — |
| 더블역만 | 국사무쌍 13면 대기 / 스안커 단기 대기 / 순정구련보등 / 대사희 | — | 토글 (기본 ON) |
- 복합역만은 합산 인정 (예: 스안커 + 자일색). — 토글
- 13판 이상 세는역만(카조에 역만) 인정. — 토글 (OFF 시 삼배만 상한)
- 오픈리치·인화·두판묶음(2판 시바리)은 구현하지 않는다.
### 2.8 점수 계산
**부(符) 계산**: 기본 20부 + 아래 가산, 최종 10부 단위 절상.
- 화료 방식: 멘젠 론 +10부, 쯔모 +2부 (핑후 쯔모 제외).
- 대기: 간짱·펜짱·단기 +2부.
- 머리: 역패 +2부. 연풍패(장풍=자풍) 머리는 4부. — 토글 (2부 옵션)
- 면자: 중장패 밍커2 / 안커4 / 밍깡8 / 안깡16, 요구패(1·9·자패)는 각 2배.
- 특례: 핑후 쯔모 = 20부 고정. 치토이츠 = 25부 고정 (절상 없음). 울고 핑후형 론 = 30부 처리.
**점수표**: 기본점 = 부 × 2^(2+판). 기본점이 2,000을 넘으면 만관으로 절삭.
| 등급 | 조건 | 기본점 | 자 론 / 친 론 |
|---|---|---|---|
| 일반 | 1~4판 (기본점 ≤2000) | 계산값 | 기본점×4 / ×6 (100 절상) |
| 만관 | 5판, 또는 기본점 2000 초과 | 2,000 | 8,000 / 12,000 |
| 하네만 | 6~7판 | 3,000 | 12,000 / 18,000 |
| 배만 | 8~10판 | 4,000 | 16,000 / 24,000 |
| 삼배만 | 11~12판 | 6,000 | 24,000 / 36,000 |
| 역만 | 13판+(카조에) 또는 역만 역 | 8,000 | 32,000 / 48,000 |
- 쯔모 지불: 자 화료 시 (친 = 기본점×2, 자 각 = 기본점×1), 친 화료 시 전원 기본점×2. 각 지불액 개별 100점 절상.
- 키리아게 만관(4판30부·3판60부를 만관 처리)은 기본 OFF. — 토글
- 본장: 1본장당 론 +300 (방총자 부담), 쯔모 +100×3.
- 검증용 확정값 예시 (테스트 픽스처에 포함할 것): 자 론 30부 4판 = 7,700 / 친 론 30부 4판 = 11,600 / 핑후쯔모 20부 4판 = 1,300·2,600 / 치토이 25부 3판 자 론 = 3,200 / 자 론 40부 2판 = 2,600.
### 2.9 유국과 연장
- **황패유국**: 패산 소진 시. 노텐벌부 총 3,000점 이동 — 1인 텐파이: 노텐 3인이 1,000씩 / 2:2: 1,500씩 / 3인 텐파이: 노텐 1인이 1,000씩 3인에게.
- **나가시만관**: 유국 시 자기 버림패가 전부 요구패이고 한 번도 울리지 않았으면 만관 상당 지급. — 토글 (기본 ON)
- 도중유국 (전부 본장 +1, 친 유지): 구종구패 (첫 순 요구패 9종 이상 시 선언 선택), 사풍연타 (첫 순 4인이 같은 풍패), 사가리치, 사깡산료, 트리플론.
- 연장(연친): 친이 화료하거나 유국 시 친이 텐파이면 본장 +1, 친 유지. 자가 화료하면 친 이동 + 본장 0. 유국으로 친 이동 시 본장은 유지된 채 +1.
### 2.10 종료 조건
- 남4국 종료 시 30,000점 이상이 없으면 서입 (서1~서4 서든데스: 누군가 30,000 도달 시 그 국으로 종료). — 토글
- **토비**: 누군가 0점 미만이 되면 즉시 종료. (0점 정확히는 속행) — 토글
- 아가리야메: 오라스에서 친이 화료/텐파이 유국으로 1위 확정 시 종료 선택 가능. — 토글 (기본 ON)
- 동점 시 기가 순 (동1국 시작 시 자리 순: 동 > 남 > 서 > 북).
---
## 3. 온라인 멀티플레이 사양
### 3.1 접속과 계정
- MVP는 **게스트 방식**: 닉네임 입력 → 서버가 세션 토큰 발급 (localStorage 보관). 재접속·전적 연결에 사용. 정식 계정/OAuth는 Phase 5.
- 저장소: 진행 중 대국은 서버 메모리, 완료 대국 로그·간단 전적은 SQLite (better-sqlite3). 스키마에는 엽전 지갑·원장 테이블(§6.4)을 처음부터 포함한다.
### 3.2 방과 매칭
- **친선방**: 6자리 코드 생성 → 코드로 참가. 방장이 룰 토글(§2) 설정, 빈자리는 봇으로 채우고 시작 가능.
- **빠른 대전**: 단일 매칭 큐 (반장전 기본룰). 4인 모이면 자동 시작, 60초 내 미달 시 봇 충원 여부를 대기자에게 선택받는다. 레이팅 매칭은 Phase 5.
- **연습**: 봇 3인과 즉시 시작 (서버 경유 동일 구조 — 코드 경로를 분리하지 않는다).
### 3.3 프로토콜 (Socket.IO, `packages/protocol`에 zod 스키마로 정의)
클라 → 서버:
| 이벤트 | 내용 |
|---|---|
| `auth.hello` | 닉네임 or 기존 토큰 |
| `lobby.quickMatch` / `lobby.cancel` | 매칭 큐 진입/취소 |
| `room.create` / `room.join` / `room.leave` / `room.ready` / `room.addBot` / `room.setRules` | 방 관리 |
| `game.action` | `{type: 'discard'|'chi'|'pon'|'kan'|'riichi'|'tsumo'|'ron'|'pass'|'kyuushu', ...}` |
| `sync.request` | 재접속 시 전체 스냅샷 요청 |
서버 → 클라:
| 이벤트 | 내용 |
|---|---|
| `room.state` | 방 상태 (멤버, 준비, 룰) |
| `game.start` | 좌석 배정, 룰 확정, **시드 해시** |
| `game.deal` | 내 손패 13장 (개인별 전송) |
| `game.event` | 공개 이벤트 브로드캐스트: 타패, 울기, 리치 선언, 신도라, 깡 등 |
| `game.draw` | 내 쯔모패 (본인에게만; 타인에게는 "쯔모했음" 사실만) |
| `game.choices` | **지금 가능한 행동 목록** (서버 계산): 예) `[{type:'ron'}, {type:'pon'}, {type:'pass'}]`, 타임아웃 정보 포함 |
| `game.roundResult` | 국 결과: 화료자 손패 공개, 역 목록, 부/판, 점수 이동, 우라도라 |
| `game.end` | 최종 순위, 우마 정산, 시드 원본 공개 |
| `sync.snapshot` | 내 시점 전체 상태 (재접속 리싱크용) |
원칙: **은닉 정보는 절대 브로드캐스트하지 않는다.** 타인 손패·패산·왕패는 서버 메모리에만 존재. `game.choices`를 서버가 내려주므로 클라이언트에는 룰 판정 코드가 필요 없다 (엔진은 서버에서만 구동, 클라는 표시용 유틸만 사용).
### 3.4 턴 타이머와 이탈 처리
- 타이머: 행동당 기본 5초 + 국당 예비시간 20초 (예비시간은 소진식). 서버가 강제.
- 시간 초과: 타패 차례면 쯔모기리, 울기 선택이면 자동 패스.
- 연결 끊김: 즉시 봇이 대체 (쯔모기리 + 전부 패스 수준의 안전 봇). 5분 내 재접속 시 `sync.snapshot`으로 복귀. 대국은 절대 중단되지 않는다.
- 자동 편의 토글 (클라 설정, 서버 반영): 자동 화료 / 울기 스킵 / 자동 쯔모기리.
### 3.5 봇 (엔진 내 `Bot` 인터페이스)
- 1단계 목표는 "규칙을 지키는 무난한 상대": 샹텐 수를 줄이는 타패 선택 (유효패 수 비교) + 리치자 등장 시 현물 우선 수비 + 1,000점 이상 확정 시 리치.
- 결정 로직도 엔진 패키지에 두어 서버·시뮬레이션 테스트에서 공용한다. 고급 AI는 Phase 5.
---
## 4. 디자인 시스템 — 한국 전통 컨셉 「청기와」
이 섹션이 이 게임의 정체성이다. 목표는 "고궁의 밤, 대청마루에 놓인 마작상" 같은 정서. 차분하고 격조 있게, 화려함은 화료 연출 같은 결정적 순간에만 터뜨린다. 모든 그래픽은 오리지널로 하되, 구조·문양·아이콘은 SVG/CSS로 그리고 질감·배경은 실사 텍스처(CC0)와 필요 시 AI 생성 이미지를 결합한다. **유니코드 이모지·특수문자를 그래픽 대용으로 쓰는 것은 전 화면에서 금지** — 아이콘 하나까지 전용 에셋으로 만든다.
### 4.1 컬러 토큰 (오방색·단청 기반)
| 토큰 | 값(시작점) | 용도 |
|---|---|---|
| `--ink` | `#191B21` | 최심부 배경, 텍스트 온 라이트 |
| `--giwa` | `#2C3040` | 기와 청회색 — 헤더, 패널 프레임 |
| `--night` | `#101528` | 로비 밤하늘 그라데이션 하단 |
| `--felt` | `#1E4B3F` | 대국 테이블 (뇌록 깊은 녹색, 가장자리로 갈수록 `#14332B` 비네트) |
| `--hanji` | `#F4EDDD` | 한지 아이보리 — 카드, 패널 배경, 밝은 텍스트 |
| `--dan-red` | `#9E3B31` | 석간주 — 경고·방총·중요 강조 |
| `--dan-orange` | `#D45D3A` | 장단 — 액티브 포인트 |
| `--dan-blue` | `#33527E` | 삼청 — 정보, 링크, 남풍 표시 |
| `--dan-green` | `#3F7E5F` | 뇌록 밝은 톤 — 성공, 텐파이 표시 |
| `--gold` | `#C8A24B` / hi `#E6C87A` | 금박 — 도라, 점수 상승, 승리 연출 |
| `--seal` | `#C03B2E` | 인주(도장) 빨강 — 콜 버튼 전용 |
실제 화면에서 명도·채도는 미세 조정하되 토큰 체계는 유지. 색만으로 의미를 전달하지 말 것 (색약 대응 — 도라는 금색 글로우 + 작은 보석 아이콘, 후리텐은 색 + "후리텐" 라벨).
### 4.2 타이포그래피
- 제목·연출 서체: **Noto Serif KR** (700/900) — 국 표시("동 1국"), 역 이름, 화료 연출. 붓글씨 느낌은 별도 폰트 없이 Noto Serif KR 900 + 스트로크 리빌 애니메이션으로 표현 (라이선스 안전).
- UI 본문: **Pretendard** (OFL). 숫자는 `font-variant-numeric: tabular-nums` (점수 정렬).
- 두 폰트 모두 로컬 번들(서브셋)로 포함, 외부 CDN 의존 금지.
### 4.3 문양·질감 (벡터 제작 + 실사 텍스처 병용)
- **기와지붕 곡선**: 로비 헤더와 모달 상단에 처마 실루엣 SVG (완만한 현수곡선 + 수막새 동그라미 반복). 게임의 시그니처 셰이프.
- **한지 질감**: 패널 배경에 미세한 종이 노이즈 (SVG turbulence 필터, 불투명도 3~5%).
- **단청 보더**: 헤더·결과 화면 테두리에 단순화한 머리초 문양(반복 SVG 패턴, 삼청+뇌록+장단 3색). 과하지 않게 2~4px 두께.
- **창호 격자**: 설정·역 일람 등 보조 패널 배경에 은은한 창살 격자 패턴.
- **조각보**: 로딩 화면 — 색면이 조각보처럼 맞춰지는 기하 애니메이션.
- **엽전**: 리치 공탁 표시는 가운데 사각 구멍 뚫린 엽전 아이콘. 점수봉 대신 점수는 숫자로, 공탁·본장만 엽전으로 시각화.
- **낙관 도장**: 치/퐁/깡/론/쯔모/리치 콜 버튼 = 인주색 사각 도장에 한지색 글자. 등장 시 "쾅" 찍히는 모션 (scale 1.35→1, 80ms 임팩트 + 잉크 번짐 효과). 이 게임에서 가장 기억에 남을 인터랙션으로 만들 것.
- **실사 텍스처**: 한지·나무(마작상 테두리)·펠트의 질감은 CC0 실사 텍스처 사진(Poly Haven, ambientCG 등)을 톤 보정해 오버레이로 깔아 "코드로 그린 티"를 지운다. 미세 노이즈만으로 부족한 곳(테이블, 패널 배경)에 우선 적용. 파일 용량은 WebP 압축으로 관리하고 출처는 `ATTRIBUTIONS.md`에 기록.
### 4.4 마작패 디자인
- 패 앞면: 오픈소스 리치마작 타일 SVG 세트 사용 (후보: FluffyStuff/riichi-mahjong-tiles 등 — **사용 전 LICENSE 확인 필수**, `ATTRIBUTIONS.md`에 기록. 라이선스가 부적합하면 표준 도안(만·통·삭·자패는 전통 공용 도안)을 직접 SVG로 그린다. 가독성이 최우선이므로 패 앞면은 표준 도안 유지).
- 패 뒷면: **오리지널** — 쪽빛(`#243B6B` 계열) 바탕에 금박 수막새 문양 1개. 게임의 아이콘이 될 디자인.
- 패 표현: 살짝의 입체감 (상단 하이라이트, 하단 2px 두께면, 부드러운 그림자). 3D 엔진 없이 CSS로.
- 적5는 도안 자체가 붉은 버전 + 미세한 금테로 구분 강화.
- **유니코드 마작 문자(🀇 등)로 패를 렌더링하는 것은 금지.** 반드시 전용 벡터 도안을 사용한다 (플레이스홀더 단계에서도).
### 4.5 모션 원칙
- 기본 트랜지션 180ms, `cubic-bezier(0.2, 0.8, 0.2, 1)`. 패 이동(타패→강)은 240ms 포물선 경로.
- 쯔모: 패가 패산 방향에서 슬라이드 인 + 손패 정렬은 FLIP 애니메이션.
- 리치 선언: 붓 스트로크로 "리치" 글자 리빌 (600ms) + 화면 가장자리 금빛 vignette 1회 맥동 + 타패가 가로로 눕는 모션.
- 화료: 손패 공개 → 역 이름이 세로 목록으로 하나씩 도장 찍히듯 등장 → 부/판 → 점수 카운트업. 만관 이상은 배경에 병풍이 펼쳐지는 연출 (3폭, 900ms), 역만은 금박 파티클 추가.
- 유국은 차분하게 (먹색 페이드 + "유국" 서예 표기).
- 모든 연출은 스킵 가능(클릭) + 설정에서 "연출 간소화" 제공.
### 4.6 사운드
- 효과음: 타패 "딱", 쯔모 슬라이드, 울기 시 북 "둥", 리치 시 대금 짧은 선율 + 북, 화료 시 가야금 글리산도, 역만 시 태평소. 전부 CC0/자체 신스 제작으로 조달하고 출처를 `ATTRIBUTIONS.md`에 기록. 적합한 국악 샘플이 없으면 MVP는 타악·신스 효과음만으로 구성 (품질 낮은 소스를 억지로 쓰지 말 것).
- BGM은 MVP 범위 외 (무음 기본). 사운드 전체 음소거·볼륨 설정 제공.
---
## 5. 화면 구성과 UX
### 5.1 화면 목록
1. **로비**: 밤하늘 그라데이션 + 궁궐 처마 실루엣. 원경에는 CC0 실사 야경이나 AI 생성 원화(§9-5 워크플로)를 어둡게 보정해 깔 수 있다. 중앙 한지 카드에 닉네임 입력, 그 아래 [빠른 대전] [친선방] [연습 대국] 세 버튼. 하단에 간단 전적.
2. **대기방**: 4개 좌석 (한지 방석 카드), 룰 요약 패널, 방 코드 복사 버튼, 봇 추가, 준비/시작.
3. **대국 화면** (아래 상세).
4. **국 결과**: 화료 손 공개 + 역/점수 연출 (§4.5). 계속 버튼.
5. **최종 결과**: 병풍 배경에 순위표, 원점수/우마 정산, 시드 검증 링크, [한 번 더] [로비로].
### 5.2 대국 화면 레이아웃 (1280×800 기준)
```
┌────────────────────────────────────────────────┐
│ [메뉴] [설정]              대면(토이멘) 손패 뒷면       │
│         버림패(대면)                                │
│ 좌측    ┌──────중앙 정보판──────┐          우측     │
│ 상가    │ 동1국 · 1본장 · 잔여 70 │          하가     │
│ 손패    │  도라표시패  [엽전×공탁]  │          손패     │
│ (세로)  │  4인 점수 + 풍 + 리치표시 │         (세로)    │
│         └─────────────────────┘                  │
│         버림패(자신)                                │
│  내 울기 면자          내 손패 13장 + 쯔모패    [콜버튼] │
└────────────────────────────────────────────────┘
```
- 버림패(강): 각자 6장×최대 4열, 리치 선언패는 가로로 눕힘. 자신 기준 상대 위치(상가=좌, 대면=위, 하가=우)로 회전 배치.
- 중앙 정보판: 기와색 패널. 현재 턴인 사람 방향의 변이 금색으로 빛나는 턴 인디케이터. 남은 예비시간은 좌석 옆 금색 링으로.
- 내 손패: 크게 (패 폭 ~48px). 호버 시 살짝 상승, 클릭으로 타패. 쯔모패는 오른쪽에 간격 두고 분리.
- 콜 버튼(도장)은 손패 우측 상단에 등장. 항상 [패스]가 가장 오른쪽·가장 수수한 디자인.
### 5.3 편의 기능 (기본 무료 제공 — 상용 게임과의 차별화 포인트)
- 텐파이 시 대기패 표시 + 대기별 **예상 점수**(론/쯔모, 우라 제외) 미리보기.
- 이 패를 버리면 텐파이가 되는 패 하이라이트 (유효패 힌트). 설정에서 끌 수 있음 (경기 모드).
- 후리텐 상태 명확 표시 (라벨 + 대기패에 취소선 뉘앙스).
- 역 일람 패널 (검색 가능, 현재 손에서 노릴 수 있는 역 상단 정렬), 국 이벤트 로그, 마지막 국 복기 화면.
- 자동 옵션 3종 (§3.4), 대국 중 좌하단 토글.
### 5.4 접근성·반응형
- 키보드: ←→로 패 선택, Enter 타패, 단축키로 콜 응답. 포커스 링 명확히.
- 명도 대비 WCAG AA 이상 (한지색 텍스트 온 기와색 검증).
- 1024~1280px에서는 스케일 축소로 대응, 모바일 세로 레이아웃은 Phase 5.
---
## 6. 엽전 경제 시스템 (설계 확정 — 구현은 Phase 5)
무료 소프트 재화 「엽전」과 코스메틱 상점의 확정 설계. 실제 구현은 Phase 5이지만, DB 스키마와 대국 결과 기록(§6.4)은 Phase 2부터 이 사양을 전제로 만들어 나중에 마이그레이션 없이 붙일 수 있게 한다.
### 6.1 원칙
1. 엽전은 **무료 재화**다. 현금 결제 없음, 유저 간 거래·양도 없음, 엽전을 거는 베팅 대국 없음 (사행성 리스크 원천 차단).
2. 엽전으로 살 수 있는 것은 전부 **코스메틱**. 승패·판정·매칭에 영향을 주는 상품은 어떤 형태로도 금지.
3. 지갑은 서버 권위: 잔액 변경은 서버만 수행하고 클라는 표시만 한다. 모든 증감은 append-only 원장(ledger)에 사유와 함께 기록.
### 6.2 획득 (초기값 — 이후 밸런스 튜닝 전제)
| 획득처 | 엽전 |
|---|---|
| 반장전 완주 기본 보상 | 100냥 (동풍전 50냥) |
| 순위 보너스 | 1위 +80 / 2위 +40 / 3위 +20 / 4위 +10 |
| 오늘의 첫 대국 | +50냥 |
| 역만 화료 기념 | +500냥 (계정당 역별 1회) |
| 빈곤 구제 | 잔액 200냥 미만이면 다음 날 200냥까지 자동 충전 |
- 지급 조건: **인간 플레이어 2인 이상** 대국만 보상 (봇 상대 어뷰징 방지). 연습 모드는 무보상, 탈주(재접속 포기) 시 무보상.
- 지급은 gameId 기준 멱등 처리 (재접속·중복 정산으로 이중 지급되지 않게).
### 6.3 사용처 — 상점 「저잣거리」 (초기 라인업)
| 카테고리 | 예시 | 가격대 |
|---|---|---|
| 패 뒷면 | 수막새(기본·무료) / 연화문 / 도깨비문 / 태극 | 800~1,500냥 |
| 테이블 | 뇌록(기본) / 쪽빛 / 먹색 / 자단 | 1,000~2,000냥 |
| 화료 연출 | 기본 / 금박 매화 / 청룡 붓선 | 2,000~3,000냥 |
| 이모티콘 팩 | 하회탈 8종 / 각시탈 8종 | 1,200냥 |
- 상점 UI는 한지 좌판 컨셉, 구매 전 미리보기 필수. 장착 상태는 프로필 단위로 저장.
- 콘텐츠 제작 부담을 고려해 초기엔 카테고리당 2~3종이면 충분하다. 전부 SVG/CSS 오리지널 제작 (§9 에셋 규칙 준수).
### 6.4 구현 메모
- SQLite 스키마: `wallets(user_id, balance)`, `ledger(id, user_id, delta, reason, game_id, created_at)`, `unlocks(user_id, item_id)`, `loadouts(user_id, slot, item_id)`.
- 대국 결과 저장 시 인간 플레이어 수·완주 여부·최종 순위를 기록해 보상 계산 근거를 남긴다 (**Phase 2부터 적용**).
- 로비 우상단에 엽전 잔액 상시 표시 (엽전 아이콘 + "1,240냥"), 획득 시 +금액 플로팅 애니메이션.
---
## 7. 개발 단계 (Phase 로드맵)
각 Phase는 "실행해서 보여줄 수 있는 상태"로 끝난다. Phase 완료 시 데모 방법과 함께 보고하고, 사용자 확인 후 다음 Phase로 진행한다.
### Phase 0 — 스캐폴드 (반나절 분량)
모노레포 셋업, 패키지 4개 골격, TS strict + ESLint + vitest, GitHub Actions로 테스트 CI, `pnpm dev`로 서버+클라 동시 기동, Dockerfile.
**완료 기준**: 클론 → `pnpm i && pnpm dev` → 브라우저에 플레이스홀더 로비가 뜬다. 테스트 1개 통과.
### Phase 1 — 마작 엔진 (프로젝트의 심장)
타일·패산·배패, 턴 상태기계, 울기 판정, 리치, 후리텐, 샹텐 계산기(일반형+치토이+국사), 야쿠 판정 전체(§2.7), 부/점수 계산(§2.8), 유국·연장·종료(§2.9~2.10), 룰 토글, 봇 v1, 리플레이용 이벤트 로그.
**완료 기준**: §8의 엔진 테스트 전부 통과 + 봇 4인 자동 대국 10,000반장 시뮬레이션에서 크래시 0, 불변식(4인 점수 + 공탁 = 100,000) 위반 0. 야쿠별 발생 통계 출력이 상식적 분포일 것 (예: 리치·역패가 최다, 역만은 극소수).
### Phase 2 — 서버와 프로토콜
Socket.IO 프로토콜(§3.3), 방/매칭/타이머/재접속/봇 대체, 게스트 토큰, SQLite 전적, 시드 커밋-공개.
**완료 기준**: 터미널 4개에서 테스트 클라이언트 스크립트로 반장전 완주. 중간에 1명 강제 종료 → 봇 대체 → 재접속 복귀 시나리오 통과.
### Phase 3 — 클라이언트 코어 (기능 우선, 임시 스타일)
로비→대기방→대국→결과 전체 플로우, 대국 화면 레이아웃(§5.2), 모든 액션 UI, 편의 기능(§5.3), 재접속 UX.
**완료 기준**: 브라우저 2개 + 봇 2로 실제 반장전을 처음부터 끝까지 플레이 가능. 룰 오류 0.
### Phase 4 — 디자인·연출 (이 게임의 얼굴)
디자인 시스템 전면 적용(§4), 문양·패 뒷면·도장 버튼 제작, 전 연출(리치/화료/유국/결과), 사운드, 60fps 튜닝, 스킵·간소화 옵션.
**완료 기준**: 첫인상에서 "상용 게임급"이라는 소리가 나올 것. 애니메이션 프레임 드랍 없음 (Performance 패널 검증), Lighthouse 성능 90+.
### Phase 5 — 확장 (본 계획서 범위 외, 이후 별도 지시)
엽전 경제와 저잣거리 상점 (§6 사양 그대로 — Phase 5의 최우선 항목), 레이팅·등급 시스템 (한국식 명칭 예: 유생→진사→급제→장원), 리플레이 뷰어, 관전, 정식 계정, 모바일 레이아웃, 이모티콘 소통 (탈 모티프, §6.3 상품과 연동), 삼마, 대회 모드.
---
## 8. 테스트와 품질 기준
### 8.1 엔진 (vitest — 가장 두텁게)
- **샹텐 계산기**: 알려진 손 최소 200케이스 (일반/치토이/국사/복합, 0~8샹텐). 소규모 무작위 손에 대해 브루트포스 검증기와 결과 대조 (differential testing).
- **야쿠 판정**: 역마다 성립 3+ / 불성립 경계 2+ 픽스처 (예: 울면 사라지는 역, 탕야오+적5, 량페코vs이페코×2, 국사 13면).
- **부·점수**: §2.8 확정값 전부 + 핑후쯔모/쿠이핑후/치토이/연풍머리/절상 경계. 점수표 전 구간 스냅샷 테스트.
- **진행 규칙**: 후리텐 3종, 리치 후 안깡 허용/불허, 창깡, 사깡산료, 더블론 공탁 귀속, 나가시만관, 노텐벌부 3분기, 아가리야메.
- **불변식 시뮬레이션**: 시드 고정 10,000반장 — 점수 합 보존, 패 총량 136 보존, 같은 시드 → 같은 결과(결정론).
### 8.2 서버·클라
- 서버: 프로토콜 통합 테스트 (정상 대국, 타임아웃, 재접속, 불법 액션 거부 — 서버가 반드시 거부하는지).
- 클라: Playwright E2E 스모크 1본 (방 생성 → 봇 3 추가 → 동풍전 완주 → 결과 확인).
- 보안 자가 점검: 다른 플레이어 손패가 네트워크 페이로드에 절대 실리지 않는지 테스트로 고정.
### 8.3 코드 품질
- TS strict, 엔진 패키지 `any` 금지, 공개 API에 JSDoc.
- 성능 예산: 초기 JS 번들 < 500KB gzip (폰트·사운드 제외), 대국 중 리렌더 최소화 (react memo/selector).
---
## 9. 에셋·라이선스 규칙 (엄수)
1. 타 상용 게임(작혼 포함)의 이미지·사운드·폰트·일러스트·고유 텍스트를 복사하거나 유사하게 모사하지 않는다. 리치마작 **규칙 자체**는 전통 게임 룰이므로 자유롭게 구현한다.
2. 외부 리소스는 라이선스 확인 → `ATTRIBUTIONS.md` 기록 → 사용. 라이선스 불명 리소스는 쓰지 않는다.
3. 마작패 앞면의 전통 도안과 수학적 규칙 표는 공용 영역으로 취급 가능. 폰트는 OFL만.
4. "청기와"는 가칭. 리포지토리 어디에도 타 게임 상표를 쓰지 않는다.
5. **이모지·유니코드 특수문자를 그래픽 대용으로 쓰지 않는다** (패, 아이콘, 버튼, 장식 전부 — 임시 UI 단계 포함). 모든 시각 요소는 전용 SVG 또는 이미지 에셋.
6. 실사 질감·배경 이미지는 CC0 소스(Poly Haven, ambientCG, Pixabay 등)에서 조달한다. 일러스트풍 원화가 필요한 경우(로비 배경 등)는 사용자가 AI 이미지 생성 도구로 만들어 `apps/client/assets/handoff/` 폴더에 넣어주면 클로드 코드가 보정·통합하는 워크플로를 따른다 — 필요한 이미지의 사양(주제, 구도, 톤, 해상도)을 목록으로 정리해 사용자에게 요청할 것. 이 이미지들도 전부 `ATTRIBUTIONS.md`에 기록.
---
## 10. 진행 방식 (클로드 코드 작업 지침)
1. **Phase 순서 엄수**. 각 Phase 완료 시: 무엇을 했는지 요약 + 실행/데모 방법 + 알려진 한계를 보고하고 잠시 멈춰 확인을 받는다.
2. 이 문서에 없는 규칙·수치를 정해야 하면 표준 리치마작 관례를 따르고 `ASSUMPTIONS.md`에 한 줄씩 기록한다. 룰 관련 결정은 반드시 엔진 테스트를 함께 추가한다.
3. 커밋은 작게, conventional commits (`feat(engine): ...`). Phase별 브랜치 후 머지.
4. 막히면 우회 구현으로 넘어가지 말고 보고할 것 (특히 룰 판정·점수계산은 "대충 맞는" 구현 금지).
5. 첫 작업: Phase 0 시작 전에 이 문서를 리포지토리 루트에 `PLAN.md`로 저장하고, RULES.md·ASSUMPTIONS.md·ATTRIBUTIONS.md 빈 파일을 만든 뒤 진행한다.
